import "Object2D", './object2d'

export default class Scene < Object2D
  NAME_SCENE = "Scene"

  def initialize
    super
    this.id = "scene_r"
  end

  def ready
    unless this.parent
      this.global_position = this.position
    end
  end
end
